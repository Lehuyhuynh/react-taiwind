import { Outlet, Link } from "react-router-dom";

const Layout = () => {
  return (
    <div>
       <img src="img/" alt="" />
       .wrapper my-14 <mx-auto></mx-auto>
    </div>
    <Outlet />
  )
};

export default Layout;